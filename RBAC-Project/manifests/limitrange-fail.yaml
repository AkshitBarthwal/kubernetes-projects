apiVersion: v1
kind: Pod

metadata:
  name: limitrange-fail
  namespace: dev

spec:
  containers:
  - name: nginx
    image: nginx

    resources:
      requests:
        cpu: "700m"
        memory: "700Mi"

      limits:
        cpu: "700m"
        memory: "700Mi"
