apiVersion: v1
kind: Pod
metadata:
  name: limitrange-fail
  namespace: dev

spec:
  containers:
  - name: nginx
    image: nginx

    resources:
      requests:
        cpu: "600m"
        memory: "700Mi"

      limits:
        cpu: "600m"
        memory: "700Mi"
