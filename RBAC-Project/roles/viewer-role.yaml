apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: viewer-role
  namespace: qa

rules:
- apiGroups: [""]
  resources:
    - pods
    - services
  verbs:
    - get
    - list
    - watch
