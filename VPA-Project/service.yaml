apiVersion: v1
kind: Service

metadata:
  name: nginx-service
  namespace: vpa-demo

spec:
  selector:
    app: nginx-vpa

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80

  type: ClusterIP
